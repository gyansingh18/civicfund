<div class="allocation-summary-container">
  <h1>Your Allocation Summary</h1>

  <ul class="allocation-list">
    <% @allocations.each do |allocation| %>
      <li>
        <strong><%= allocation.project.name %></strong> — $<%= allocation.amount %>
      </li>
    <% end %>
  </ul>

  <div class="allocation-totals">
    <p><strong>Total Allocated:</strong> $<%= @allocations.sum(&:amount) %></p>
    <p><strong>Remaining Budget:</strong> $<%= 1000 - @allocations.sum(&:amount) %></p>
  </div>

  <%= link_to "Back to Projects", projects_path, class: "btn-back" %>
</div>
